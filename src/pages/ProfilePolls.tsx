import { useContext } from "react";
import { ProfileContext } from "../contexts/ProfileContext";

export default function ProfilePolls() {
  const profile = useContext(ProfileContext);

  return null;
}
